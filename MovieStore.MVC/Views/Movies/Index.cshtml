@model IEnumerable<MovieStore.Core.Entities.Movie>


<div>
    <div class="container-fluid">
        <div class="row">
            @foreach (var movie in Model)
            {
                @movie.Title  <br />

            }
        </div>
    </div>
</div>